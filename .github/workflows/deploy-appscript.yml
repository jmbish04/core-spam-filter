name: Deploy Apps Script

on:
  push:
    branches:
      - main
    paths:
      - 'appscript/**'
  workflow_dispatch:
      inputs:
        target_project:
          description: 'Which project to deploy to'
          required: true
          type: choice
          default: 'both'
          options:
            - both
            - 126colby
            - gmail

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install clasp
        run: npm install -g @google/clasp

      - name: Create ~/.clasprc.json
        run: |
          echo '${{ secrets.CLASPRC_JSON }}' > ~/.clasprc.json

      - name: Deploy to both environments
        env:
          APPSCRIPT_PROJECT_ID_GMAIL: ${{ secrets.APPSCRIPT_PROJECT_ID_GMAIL }}
          APPSCRIPT_PROJECT_ID_126COLBY: ${{ secrets.APPSCRIPT_PROJECT_ID_126COLBY }}
        run: |
          chmod +x scripts/deploy-appscript.sh
          bash scripts/deploy-appscript.sh
#       - name: Deploy to 126COLBY Project
#         run: |
#           # Replace the placeholder with the actual secret ID
#           sed -i 's/YOUR_APPS_SCRIPT_ID/${{ secrets.APPSCRIPT_PROJECT_ID_126COLBY }}/g' .clasp.json
#           clasp push --force
#           # Revert the file back to its original state from the repository
#           git checkout .clasp.json

#       - name: Deploy to GMAIL Project
#         run: |
#           # Replace the placeholder with the second secret ID
#           sed -i 's/YOUR_APPS_SCRIPT_ID/${{ secrets.APPSCRIPT_PROJECT_ID_GMAIL }}/g' .clasp.json
#           clasp push --force
